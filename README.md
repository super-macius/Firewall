# Firewall 🔥🧱🐍
Firewall written in Python, not a real one, just simulator.

# How does it work?
1. Generate random IP numbers
2. Compare IP number with block list
3. If was not on the list, gets allow state
